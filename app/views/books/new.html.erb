<h2>Add a Book</h2>

<%= form_for @book, class: "form-horizontal center" do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
    <%= f.hidden_field :rank, value: 0 %>
    <div class="span7 text-center">
      <%= f.submit "Add Book", class: "btn btn-default" %>
    </div>
<% end %>

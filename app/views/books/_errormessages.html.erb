<% if @book.errors.any? %>
  <div class="alert alert-danger" role="alert">
  <ul class='error-messages'>
    <% @book.errors.each do |column_name, message| %>
      <li><%= column_name.upcase %> <%= message %></li>
    <% end %>
  </ul>
  </div>
<% end %>
